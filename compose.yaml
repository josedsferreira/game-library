services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=gamesDB'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=josedsferreira'
    ports:
      - '5432:5432'
